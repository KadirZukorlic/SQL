-- SELECT DISTINCT booking_date, SUM(num_guests)
-- FROM bookings; -- Doesn't work because DISTINCT duplicate values only

-- SELECT booking_date, SUM(num_guests)
-- FROM bookings
-- GROUP BY booking_date;
